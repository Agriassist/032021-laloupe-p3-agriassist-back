// const BTRouter = require('express').Router();

// const { getAllBT, getOneBTById, createOneBT, deleteOne } = require('../controllers/fiche_technique.controller');

// BTRouter.get('/', getAllBT);
// BTRouter.get('/:id', getOneBTById);
// BTRouter.post('/', createOneBT, getOneFactureById);
// BTRouter.delete('/:id', deleteOne);

// module.exports = BTRouter;
